# 코드 구조와 변경 지침

이 프로젝트는 Bun과 OpenTUI로 만든 Git worktree 및 개발 서버 관리 도구다.
CLI와 TUI는 같은 `Manager`를 통해 작업하며, 서버는 TUI와 별도 프로세스에서 실행된다.

## 책임과 의존 방향

- `cli.ts`는 명령 해석과 출력을 담당하고 `Manager`를 호출한다.
- `app.ts`는 화면을 조립하고 현재 프로젝트·worktree·포트를 선택한다. 비동기 작업의
  중복 실행 방지, 새로고침, 상세 정보 갱신 순서도 여기서 관리한다.
- `ui/`는 화면 표시와 입력을 담당한다. 작업할 때는 전달받은 콜백을 호출하며,
  `Manager`를 생성하거나 상태 파일을 수정하지 않는다. 브랜치 선택기는 목록만 조회한다.
- `manager.ts`는 작업 검증과 상태 변경을 묶는 진입점이다. 서버 실행·종료와 worktree 삭제는
  `Store.edit`의 잠금 안에서 수행한다.
- `managed-run.ts`는 실행 파일 경로, 준비 확인, 실행 토큰, 종료 요청과 로그 파일을 처리한다.
  이 모듈을 호출하기 전 실행 가능 여부를 검증하고 필요한 잠금을 확보하는 책임은 `Manager`에 있다.
- `workspace-snapshot.ts`는 Git 상태, 저장된 실행 정보, 실제 프로세스와 포트를 합친다.
- `desktop.ts`는 외부 앱 실행을 담당한다. `Manager`의 기존 실행 메서드는 이 모듈을 호출한다.
- `git.ts`, `process-info.ts`, `store.ts`, `projects.ts`는 각각 Git, 운영체제 프로세스,
  저장소별 실행 상태, 사용자 프로젝트 등록 목록의 구현을 담당한다.

화면 구현이 상위 `WorktreeApp` 객체 전체를 받거나 상속하지 않도록 한다. 표시할 상태와 실제로
필요한 콜백만 전달하면 화면 수정이 서버 작업과 저장 형식으로 전파되는 것을 막을 수 있다.

## 기능을 바꿀 때 찾을 위치

| 변경 대상 | 수정할 모듈 |
| --- | --- |
| 작업 실행, 단축키, 상세 정보와 화면 배치 | `src/app.ts` |
| 버튼 표시·선택·클릭·키보드 활성화 | `src/ui/controls.ts` |
| 입력창 추가, 검증 문구, 취소와 포커스 복원 | `src/ui/dialogs.ts` |
| 브랜치 검색, 실제 Git ref 선택 | `src/ui/branch-picker.ts` |
| 프로젝트 행, worktree 표시, 방향키 탐색 | `src/ui/project-navigator.ts` |
| 툴팁 위치·Unicode 표시 | `src/ui/project-tooltip.ts` |
| 알림 유지 시간과 오류 우선순위 | `src/ui/notifications.ts` |
| 공통 색상 | `src/ui/theme.ts` |
| 실행·삭제 허용 조건과 저장 | `src/manager.ts` |
| 감독 프로세스와의 통신 | `src/managed-run.ts`, `src/supervisor.ts` |
| 상태 표시와 Managed/External 포트 연결 | `src/workspace-snapshot.ts` |
| 외부 프로그램 실행 | `src/desktop.ts` |

## 보존해야 하는 규칙

1. 실행 여부 확인, 서버 시작, 실행 기록 저장은 같은 잠금 안에서 이루어진다. 실행 준비 확인을
   잠금 밖으로 옮기면 동시에 두 개의 서버가 시작될 수 있다.
2. PID만으로 서버를 식별하지 않는다. 시작 시각을 함께 확인하고 현재 실행의 토큰으로 종료를
   요청한다. 이전 Python 기록의 식별 검사와 저장 필드 이름도 유지한다.
3. 앱 종료나 프로젝트 전환은 서버를 종료하지 않는다. 외부 서버는 표시와 삭제 보호에만 사용한다.
4. 관리 중인 프로세스의 하위 관계로 확인한 포트를 먼저 연결한다. 외부 포트는 작업 경로가
   포함된 가장 깊은 worktree에 연결한다. 상태 우선순위와 조회 오류 경고를 유지한다.
5. 새 브랜치의 표시 이름과 Git ref를 구분한다. 원격 기준 브랜치가 새 브랜치의 upstream이 되지
   않도록 `--no-track`을 유지하고, 기존 브랜치는 기존 upstream을 보존한다.
6. 닫힌 다이얼로그나 이전 프로젝트에서 시작한 비동기 결과가 현재 화면을 바꾸면 안 된다.
   다이얼로그 식별, 프로젝트 식별, 상세 갱신 순번과 dispose 검사를 유지한다.
7. 텍스트 선택과 패널 크기 조절 중의 mouse-up은 버튼 동작으로 이어지면 안 된다.
8. 오류 알림은 성공·진행 알림이 덮어쓰지 않는다. Esc는 드롭다운, 다이얼로그, 오류 알림 순서로 처리한다.
9. `managed-run.ts`의 감독 프로세스 경로는 TypeScript 소스, JavaScript 번들, 독립 바이너리에서
   모두 작동해야 한다. 파일 위치나 빌드 진입점을 변경하면 반드시 빌드 검증을 실행한다.

## 검증

```sh
bun run typecheck
bun run check
bun run test
bun run verify:build
```

테스트는 임시 Git 저장소와 실제 로컬 서버를 사용한다. 프로세스 조회와 로컬 포트 열기가 허용된
환경에서 실행한다. `tests/compatibility.test.ts`는 기본 명령 선택 순서, 오류 문구, 종료 상태,
로그의 바이트 단위 마지막 부분 읽기, CLI 출력 형식을 검증한다.

화면 리팩터링에는 `bun scripts/verify-ui-compatibility.ts /path/to/old/src/app.ts`를 추가로 사용할 수
있다. 이전 소스도 현재 프로젝트와 같은 OpenTUI 의존성을 사용해야 한다. 이 비교는 임시 저장소의
동일한 경로와 상태에서 80·94·95·120·160열 화면을 실행하고 문자, 색상, 커서, 포커스와 선택을 비교한다.
실제 생성·삭제·서버 실행·취소·늦게 도착한 응답의 검증은 기존 통합 테스트가 담당한다.
